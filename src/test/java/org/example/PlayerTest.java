package org.example;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PlayerTest {

    @Test
    public void tesTMove() {
        //Board board = new Board();
        //board.createTiles();
        //Piece piece = new Piece(board.getTile(0), board);
        //Player player = new Player("lars", piece);
        //assertEquals(0, player.getPiece().getTile().getIndex());
        //player.move(5);
        //assertEquals(5, player.getPiece().getTile().getIndex());
    }

}